apps:
  - name: minze-server
    script: ./dist/index.js
    instances: max
    exec_mode: cluster
    env:
      NODE_ENV: development
    env_staging:
      NODE_ENV: production
    env_production:
      NODE_ENV: production
